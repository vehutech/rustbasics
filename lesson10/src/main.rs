// Constants
// Constants are values that are bound to a name and are not allowed to change.
// You are not permitted to add mut to a const. They are never immutable.

fn main() {
    println!("Hello, world!");
}
